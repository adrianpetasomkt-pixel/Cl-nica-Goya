node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*

# build intermediário da demonstração (regerar com: npm run demo)
.demo-build

# capturas da auditoria (regerar com: npm run auditar)
demo/auditoria

# O arquivo de demonstração É versionado, ao contrário do resto do build:
# ele é o entregável que vai para o cliente, é autocontido e precisa estar
# recuperável sem rodar o projeto. Regerar com: npm run demo
